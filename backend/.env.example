# Server
PORT=8080
HOST=0.0.0.0
CORS_ORIGIN=*
LOG_LEVEL=info

# Database
DATABASE_URL=postgres://thegame:thegame@localhost:5432/thegame

# Auth
JWT_ACCESS_SECRET=
JWT_ACCESS_TTL_SECONDS=900
REFRESH_TTL_DAYS=30
SESSION_ABSOLUTE_TTL_DAYS=365
PASSWORD_RESET_TTL_MINUTES=30

# SMTP (envio de email)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM_NAME=The Game
SMTP_FROM_EMAIL=no-reply@thegame.local

# Google Cloud Storage
GCS_PROJECT_ID=
GCS_CLIENT_EMAIL=
GCS_PRIVATE_KEY=
GCS_BUCKET=
GCS_PUBLIC_BASE_URL=
